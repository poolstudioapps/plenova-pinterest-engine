/** Shared constants used by more than one module. */
export const OAUTH_STATE_COOKIE = "pinterest_oauth_state";

/** PKCE state + verifier, held between the TikTok redirect and the callback. */
export const TIKTOK_STATE_COOKIE = "tiktok_oauth_state";
export const TIKTOK_VERIFIER_COOKIE = "tiktok_oauth_verifier";
